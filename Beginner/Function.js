// Use of Function
function myFun(x, y) {
  return 1 + (x + y) / 2;
}

  let x = 10;
  let y = 20;
  let z = 30;
  console.log ("The Average and one plus in x an y is ", myFun(x , y));
  console.log ("The Average and one plus in y an z is ", myFun(y , z));
  console.log ("The Average and one plus in x an z is ", myFun(x , z));

