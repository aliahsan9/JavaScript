// Make a Validation Form by JS
let num = prompt("Enter the Digits from between 1 and 10");
if(num>=1 && num<=9){
console.log("Valid Input.");
}else{
    console.log("Your Input is Invalid!");
}